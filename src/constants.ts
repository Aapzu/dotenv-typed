export const OPTIONAL_STRING = { type: String, optional: true } as const
export const OPTIONAL_NUMBER = { type: Number, optional: true } as const
export const OPTIONAL_BOOLEAN = { type: Boolean, optional: true } as const
